import { OverFileSizeParams } from './types';
/**
 * @description 타겟 파일 사이즈가 기준 파일 사이즈 이상 / 초과인지 판별해주는 함수
 * @returns 이상 / 초과라면 true 아니라면 false
 */
export declare const isOverFileSize: ({ fileSize, criterialFileSize, criterialFileSizeUnit, hasEqualSign }: OverFileSizeParams) => boolean;
