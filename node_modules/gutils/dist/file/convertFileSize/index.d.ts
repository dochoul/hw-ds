/**
 * @description 타겟 파일 사이즈에 맞는 파일 사이즈 단위로 변환해주는 함수
 * @param fileSize 변환될 타겟 파일 사이즈
 * @param numberOfDecimalPoints 변환 시 반환할 값의 소수점 개수
 * @returns 변환된 파일 사이즈 (ex. { size: '100.00', unit: 'MB' })
 */
export declare const convertFileSize: (fileSize: number, numberOfDecimalPoints: number) => {
    size: string;
    unit: string;
};
