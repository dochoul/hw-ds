/**
 * @description 사용 가능한 정규 표현식 플래그만 추출하는 함수
 * @param flags 정규 표현식 플래그 문자열
 * @returns 사용 가능한 플래그만 남긴 정규 표현식 플래그 문자열
 */
export declare const processUniqueSupportedRegExpFlags: (flags: string) => string;
